name: demo upStream
on: [pull_request]
jobs:
 cypress-run:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.2
      - name: Cypress.io
        uses: cypress-io/github-action@v2.9.7
        with:
          start: npm start
      
